%h1.task-heading タスク一覧

%h3.heading-title Add task
= form_with model: @task do |f|
  .field
    = f.text_field :text
  .field
  = f.submit "Create", class: "btn btn-add"
%br

- if @tasks.any?
  %table{"cellspacing": "0"}
    %tr#add-field
      %th Task
      %th created_time
      %th updated_time
      %th
    - @tasks.each do |task|
      %tr
        %td
          = task.text
        %td
          = task.created_at.strftime("%F %T")
        %td
          = task.updated_at.strftime("%F %T")
        %td
          = link_to "Delete", task_path(task), method: :delete, data: {confirm: "Are you sure?"}, remote: true, class: "btn btn-danger delete"